const {
  expect
} = require('chai');
const Battle = require('../battle.js');

describe.only('Battle properties'() => {
  describe('Battle properties'() => {

    it('should be an object which has a single trainer', () => {
      const
    })
  })
});